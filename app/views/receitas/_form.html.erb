<%= form_for(@receita) do |f| %>
  <% if @receita.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receita.errors.count, "error") %> prohibited this receita from being saved:</h2>

      <ul>
      <% @receita.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo, class: 'form-label' %>
    <%= f.text_field :titulo, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :descricao, class: 'form-label' %>
    <%= f.text_area :descricao, class: 'form-control' %>
  </div>

    <%= f.label :categorias %><br>
    <% Categoria.all.each do |c| %>
        <%= c.nome %>
        <%= f.check_box(:categorias, { :multiple => true }, c) %> <br>
    <% end %>
  <div class="actions">
    <%= f.submit 'Salvar', class: 'btn btn-success' %>
  </div>
<% end %>
